refactor(knowledge): redesign knowledge management section

New layout for the "知识库管理" section:
- Status chips + rebuild button at the top (always visible)
- "添加知识" collapsible section below, default collapsed
  - Drop zone (drag .json file)
  - Accordion: single-doc add (简洁模式)
  - Accordion: JSON batch import (JSON 模式)
- Document list below (unchanged)
- Search section below (unchanged)

Also fixed document title overflow: added flexShrink: 1, minWidth: 0
to the title Typography so long titles wrap instead of overflowing.
