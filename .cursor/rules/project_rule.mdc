---
alwaysApply: true
---

## Project-Specific Rules

### 当前阶段纪律
- 当前仓库采用 phase 推进方式。
- 在 `blueprints/phase0/bootstrap.md` 明确要求完成之前，不要提前进入 phase1 及之后的实现阶段。
- phase0 的重点是：
	- 创建项目骨架
	- 审计旧项目 `VoiceL2D`
	- 理解新需求
	- 编写规划与架构文档
- 在 phase0 中，不要提前迁移旧业务逻辑，不要接入真实第三方服务，不要大规模编写正式业务代码。

### 文档优先级补充
- 执行任务前，优先阅读：
	1. `blueprints/phase0/bootstrap.md`
	2. 当前相关 phase 的 blueprint 文档
	3. 项目内已有说明文档
	4. 官方文档
- 如果 blueprint 与临时口头印象冲突，以 blueprint 为准。
- 如需偏离 blueprint，必须在汇报中明确说明原因。
- 在当前项目中，开始任何阶段性工作前，优先阅读对应的 `blueprints/phaseX/` 文档。
- 若存在 `bootstrap.md` 或当前 phase 的入口文档，应先遵循该文档。

### 本项目默认技术路线
- 本项目默认使用：
	- `pnpm` 作为包管理器
	- `Tauri` 作为桌面宿主
	- `React + TypeScript` 作为前端技术栈
	- `Vite` 作为前端构建工具
- 不要擅自改为其他包管理器、宿主框架或前端框架，除非文档明确要求。

### 本项目架构偏好
- Tauri / Rust 主要负责：
	- 桌面宿主
	- 窗口管理
	- IPC / 系统能力桥接
	- 打包与本地集成
- 核心业务逻辑默认优先放在 TypeScript 中，而不是默认放入 Rust。
- 不要因为旧项目历史原因机械保留 Python。
- 只有当某部分明确强依赖 Python 生态、迁移成本过高或短期收益过低时，才考虑保留 Python。

### 迁移原则
- 本项目迁移的是“核心能力”，不是“旧目录结构”。
- 不要简单复制旧项目 `VoiceL2D` 的前后端拆分方式。
- 不要为了形式统一而照搬旧 Python 服务。
- 应优先识别并保留以下能力：
	- 语音输入链路
	- ASR → LLM → TTS 主链路
	- 表情 / 动作触发
	- 角色状态反馈
	- 控制面板与人工干预
	- 日志与调试
	- 外部事件输入
	- 紧急停止与人工接管

### 角色渲染与直播输出约束
- 角色显示系统应尽量采用“单一渲染真源”思路。
- 监控界面中的角色预览与直播输出应尽量同源，避免双实例不同步。
- 不要在没有明确设计说明的情况下，创建两个互相独立的角色状态源。

### RAG 与商品消息约束
- 本项目需要预留知识接入层，但 phase0 不要求实现完整 RAG。
- 在设计中必须区分：
	- 长期知识（角色设定、商品基础资料、FAQ 等）
	- 临时高优先级商品消息（当前主推、库存变化、活动口径等）
- 临时商品消息的优先级应高于长期知识。
- 不要把“商品消息插入”简单等同为普通知识库文档写入。

### 目录与文档约定
- phase 文档统一放在 `blueprints/phaseX/` 下。
- 阶段性执行前，先阅读对应 phase 文档。
- 阶段性执行后，在 `dev-reports/` 中输出简洁汇报。
- 若做出重要架构决策，应同步更新相关 blueprint 或架构文档，而不是只改代码不改文档。

### 实施方式
- 先审计，再规划，再搭骨架，再逐模块迁移。
- 不要跳阶段，不要在缺少文档与边界定义时直接大规模实现。
- 每次实现应尽量：
	- 范围清晰
	- 可审查
	- 可回滚
	- 便于后续 phase 延续

### 文档分层约定
- `blueprints/phaseX/`：用于记录阶段目标、范围、约束、交付物、执行顺序，不承载过多实现细节。
- `docs/research/`：用于记录外部资料调研、官方文档阅读结论、平台限制、接口规范分析等。
- `docs/migrations/`：用于记录旧系统能力审计、关键功能迁移分析、保留/重写判断、迁移风险与方案。
- `dev-reports/`：用于记录本次完成了什么、改动了什么、测试了什么、风险和待办，不作为长期设计文档的唯一载体。
- 如果某项结论会长期影响项目结构，应优先沉淀到 blueprint 或 docs 中，而不是只写在 dev-report 里。

### 关键能力迁移与外部调研留痕
- 当任务涉及迁移旧项目关键能力时，应在 `docs/migrations/` 中记录：
	- 旧实现的职责与位置
	- 新项目是否保留、重写或暂缓
	- 原因、风险与影响范围
- 当任务涉及外部平台、官方接口、框架文档或第三方系统约束时，应在 `docs/research/` 中记录：
	- 参考来源
	- 提炼出的关键约束
	- 对当前项目设计的影响
	- 尚未确定的部分
- 不要只把这些内容散落在聊天记录或 dev-report 中。