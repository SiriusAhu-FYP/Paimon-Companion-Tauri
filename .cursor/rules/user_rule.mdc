---
alwaysApply: true
---

# User Rules

## 交流与输出语言
- 默认始终使用中文交流、解释、汇报与注释。
- 如果项目本身明显以英文为主（例如 README、注释、UI 文案、接口约定均为英文），则遵循项目语言环境，避免强行中文化。
- 除非项目语境明确要求英文，否则不要主动切换为英文。

## 文档优先
- 当任务涉及框架、库、API、工程结构、配置方法、版本差异或不确定实现时，优先查阅文档，而不是直接猜测。
- 文档查阅优先级如下：
	1. 项目内的 `blueprints/` 文件夹
	2. 所使用库的官方文档
	3. 所提到技术的官方文档
- 如果文档与经验冲突，以文档和项目现有实现为准。
- 如果无法确认，应明确说明不确定点，不要假装确定。

## 工程结构倾向
- 本人重视多人协作、长期维护、清晰分层与一致性。
- Python 项目默认参考 Google 风格，优先追求：
	- 清晰命名
	- 明确职责划分
	- 可读性高于炫技
	- 稳定、易审查、易维护
- Web 项目默认参考 Vercel / Next.js 风格，优先追求：
	- 贴近框架推荐结构
	- 按路由、功能或模块自然分层
	- 尽量遵循主流工程约定
	- 减少随意自创目录结构
- 不要为了“看起来高级”而过度设计。
- 如果项目已有成熟结构，优先遵循现有结构，而不是强行重构成新的模板。

## 缩进与注释
- 默认使用 Tab 缩进。
- 行长度不做硬性限制，但应保持可读性。
- 注释默认使用单行注释；只有在确有必要时才使用多行注释。
- 注释应解释“为什么”或“作用”，不要写低价值废话。

## 开发汇报
- 每次完成一次明确任务、改动或阶段性工作后，都应生成一个 Markdown 汇报文件。
- 汇报文件统一放在 `dev-reports/` 文件夹下。
- 如果该文件夹不存在，可以创建。
- 汇报内容应简洁清楚，通常包括：
	- 本次完成了什么
	- 改动了哪些关键文件或模块
	- 新增了哪些功能
	- 是否运行了测试，运行了哪些测试
	- 是否存在风险、限制或待办事项
- 汇报不需要写成流水账，应方便后续回顾和团队协作。

## Git 与提交规范
- 每个新阶段（phase）或者重要功能开发都需要单独开一个分支，分支名格式为 `feature/<feature-name>`。
- 分支开发完毕后，在许可下需要PR回main分支，不删除原分支。
- 所有 commit message 都必须使用 Conventional Commits。
- commit 应尽量包含 scope（模块名或功能范围）。
- 只有影响较大、风险较高或改动面较广时，才补充较详细的 body。
- 不要生成随意、模糊、无意义的提交信息，例如 `update`, `fix stuff`, `changes`。
- 如果我要求提交代码，应优先给出规范的 commit message 建议。

## 测试要求
- 只要新增了新功能，就应运行对应功能模块的测试。
- 测试应尽量复用工程内已有的函数、模块和调用路径。
- 不要在测试中按照“同样思路重新写一遍逻辑”来伪装测试通过。
- 测试应验证真实使用路径，而不是脱离工程实现单独造一个平行版本。
- 在 `dev-reports/` 中记录：
	- 新增了哪些功能
	- 跑了哪些测试
	- 哪些内容没有测试
	- 测试结果如何

## 包管理与环境管理
- 不要擅自切换项目已有的包管理工具或虚拟环境管理方式。
- 只有当任务说明、项目文档或现有工程明确指定使用 `pnpm` 时，才使用 `pnpm`。
- 允许根据任务需要自行创建环境，但要遵循项目上下文，不要无故迁移工具链。
- 不要把“顺手统一工具”当成默认动作。

## 修改原则
- 优先做最小必要改动，避免顺手改动无关代码。
- 优先遵循现有项目的代码风格、模块边界和命名方式。
- 如果任务会影响多个文件、模块边界或整体结构，先给出简短计划再执行。
- 不要为了追求所谓“最佳实践”而破坏现有项目稳定性。
- 当项目已有明确实现方式时，优先与现有工程保持一致。

## 多人协作导向
- 所有改动都应以“便于团队理解、便于 review、便于交接”为目标。
- 优先选择清晰、稳定、常见、可维护的方案，而不是个人风格很强的方案。
- 新文件、新模块、新目录的命名应直观明确。
- 避免制造只有当前上下文才能理解的隐式约定。
- 代码、测试、汇报都应服务于协作，而不是只为当前一次性完成任务。