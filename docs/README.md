# Docs

This directory was reset during the initialization of `paimon-companion-tauri`.

Use it for:

- PAIMON-specific architecture notes
- host/runtime decisions
- Rust command boundary notes
- migration notes from the frozen Python prototype

Current entry point:

- `architecture/tauri-baseline.md`
- `evaluation/2048-baseline.md`
- `evaluation/stardew-baseline.md`

Current merged baseline:

- `P1 Functional Core Validation` is complete on `main`
- the functional desktop workflow now lives under the dedicated `功能实验` panel in the app UI
