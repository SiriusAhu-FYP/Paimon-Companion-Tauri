# services/runtime/

运行时控制器。

## 职责

- 全局运行状态管理（auto / manual / stopped / paused）
- 串行处理规则——同一时间只处理一个用户输入
- 锁麦 / 解锁协调
- 急停 / 恢复
- 人工接管优先级与切换
- 自动链路与人工链路之间的协调
- 决定当前是否允许系统继续自动播报和响应事件

## 核心原则

事件总线负责"发生了什么"，运行时控制器负责"当前允许系统怎么动"。
